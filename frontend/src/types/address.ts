export interface TAddress {
  id: string;
  receiverName: string;
  phone: string;
  province: string;
  ward: string;
  detailAddress: string;
  isDefault: boolean;
  createdAt: Date;
  updatedAt: Date;
}
